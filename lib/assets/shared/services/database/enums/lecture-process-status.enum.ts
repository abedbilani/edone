export enum STATUS {
    NOT_STARTED = 0,
    IN_PROGRESS = 1,
    COMPLETED = 2,
    FAILED = 3,
}

export enum LECTURE_STEPS {
    TRANSCRIPT = 'transcript',
    SUMMARY = 'summary',
    FLASHCARD = 'flashCard',
    QUIZ = 'quiz',
}
